# com.spontancombust.workoutoclock.dto

This package should only be used in the context of controllers and any other closely associated module.
As the name implies, DTOs should only be used to **transfer** data to and from outside the application.